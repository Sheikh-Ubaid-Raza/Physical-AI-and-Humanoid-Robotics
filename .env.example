# Environment Variables for Physical AI & Humanoid Robotics RAG Chatbot Advanced Features

# JWT Configuration
# Secret key for JWT token generation (must be 32+ characters)
JWT_SECRET_KEY=your_32_character_secret_key_here_replace_this
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=24

# Google Gemini API Configuration
# Obtain from: https://aistudio.google.com/
# Required for generating responses and creating embeddings (replaces OpenAI)
GEMINI_API_KEY=your_gemini_api_key_here
MODEL_NAME=gemini-2.5-flash
EMBEDDING_MODEL=embedding-001

# Qdrant Vector Database Configuration
# Obtain from: https://cloud.qdrant.io/
# Required for storing and retrieving vector embeddings (768 dimensions for Gemini)
QDRANT_URL=your_qdrant_cluster_url
QDRANT_API_KEY=your_qdrant_api_key

# Neon Postgres Database Configuration
# Obtain from: https://neon.tech/
# Required for storing user accounts, personalized content, translations, and conversations
NEON_DATABASE_URL=your_neon_postgres_connection_string

# Rate Limiting Configuration
RATE_LIMIT=100  # requests per minute per user

# Application Configuration
API_BASE_URL=http://localhost:8001
FRONTEND_URL=http://localhost:3000

# Additional Optional Variables
# Port to run the backend server (default: 8001 for advanced features)
# BACKEND_PORT=8001

# Timeout for API requests in seconds (default: 30)
# REQUEST_TIMEOUT=30

# Maximum number of results to retrieve from vector database (default: 5)
# TOP_K_RESULTS=5

# Confidence threshold for content relevance (default: 0.6)
# CONFIDENCE_THRESHOLD=0.6